import type { PixelColor, Util } from "@/types";

export const EMPTY_PIXEL_COLOR: PixelColor = undefined;
export const DEFAULT_COLOR: PixelColor = "#000000";
export const DEFAULT_TOOL: Util = "pen";
export const DEFAULT_FILE_NAME = "pixelspace";
